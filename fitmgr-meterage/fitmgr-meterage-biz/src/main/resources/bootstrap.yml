server:
  port: 6041

spring:
  application:
    name: @artifactId@
  # 配置中心
  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      uri: http://fitmgr-config:8888/
#      discovery:
#        enabled: true
#        service-id: fitmgr-config
  autoconfigure:
    exclude: org.springframework.cloud.gateway.config.GatewayAutoConfiguration,org.springframework.cloud.gateway.config.GatewayClassPathWarningAutoConfiguration
  profiles:
    active: dev

  # rabbitmq配置bus总线
#  rabbitmq:
#    host: 192.168.1.174
#    port: 25672
#    username: admin
#    password: admin

#logging:
#  level:
#    com.fitmgr.meterage.mapper: debug

xxl:
  job:
    admin:
      addresses: http://fitmgr-job:8080/fitmgr-job
    accessToken:
    executor:
      appname: fitmgr-meterage-biz
      address:
      ip:
      logpath: /data/applogs/xxl-job/jobhandler
      logretentiondays: 30